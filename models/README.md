# Models

- `trained/`: promoted serialized model objects.
- `checkpoints/`: model-specific training checkpoints.
- `weights/`: standalone learned weights.

Generated binaries are ignored by Git. Store the matching configuration, dependency lock, metrics, random seed, data version/checksum, and artifact checksum with each experiment record.
