# Generated artifacts

Run-generated outputs belong here, including cleaned datasets, train/test splits, SHAP values, feature importance, serialized models, and diagnostic images. Contents are ignored by Git and must be reproducible from versioned data, code, configuration, and dependency pins.

Use run-specific subdirectories rather than placing every output directly at this level.

