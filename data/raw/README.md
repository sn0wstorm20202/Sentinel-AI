# Raw data

The master dataset is `bank_fraud_dataset.csv`. Raw files are immutable: never modify or overwrite them. Register each file in `../manifest.csv` with a SHA-256 checksum and version it through Git LFS.
