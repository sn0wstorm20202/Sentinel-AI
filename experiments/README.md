# Experiments

Each experiment should have a stable identifier and capture its timestamp, Git commit, dataset checksum/version, configuration, random seed, runtime, metrics, and output artifact paths.

